# r-offp

R analyses for OFF-period analysis project

## Setup

See [environments.md](dev_docs/environments.md) and [IDEs.md](dev_docs/IDEs.md).
We are currently using option (2.1): `pixi global install` of `R` and others CLIs,
with `R` package management and locking provided by `renv`. It's okay.
