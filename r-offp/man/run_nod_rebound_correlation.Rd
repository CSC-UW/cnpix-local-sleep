% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_analysis.R
\name{run_nod_rebound_correlation}
\alias{run_nod_rebound_correlation}
\title{Run one NOD-activity vs NREM.Rebound correlation analysis}
\usage{
run_nod_rebound_correlation(
  metric,
  off_type,
  model_def,
  output_dir,
  predictor_condition = PREDICTOR_CONDITION,
  rebound_post = REBOUND_POST,
  rebound_baseline = REBOUND_BASELINE,
  response_metric = metric,
  data = NULL,
  response_data = NULL,
  save_ggplot_rds = TRUE
)
}
\arguments{
\item{metric}{Predictor (x) metric column name (\code{"total_area_norm"},
\code{"rate"}, ...).}

\item{off_type}{One of \link{OFF_TYPES}.}

\item{model_def}{Model definition list with \code{name} and \code{re_terms}.}

\item{output_dir}{Base directory; a \verb{<model_def$name>/} subdir is created.}

\item{predictor_condition, rebound_post, rebound_baseline}{Condition names (see
\code{\link[=build_correlation_frame]{build_correlation_frame()}}).}

\item{response_metric}{Response (y) metric column name (default \code{metric}, the
self-rebound case). When it differs, \code{y} is that metric's \code{NREM.Rebound}.}

\item{data}{Optional pre-loaded correlation input for \code{off_type}.}

\item{response_data}{Optional pre-loaded response frame carrying
\code{response_metric} at the rebound conditions. When \code{response_metric} differs
from \code{metric} and this is \code{NULL}, the band-power condition means are loaded
via \code{\link[=load_bandpower_condition_means]{load_bandpower_condition_means()}}.}

\item{save_ggplot_rds}{If \code{TRUE}, also save the ggplot object as \code{.rds}.}
}
\value{
Invisibly, the full result list (also saved as \code{results.rds}).
}
\description{
Loads (or accepts) the correlation input, builds the per-combo frame, fits the
full/null mixed models for \code{model_def$re_terms}, tests the slope, plots, and
writes \code{results.rds}, \code{results.json}, \code{summary.txt}, \code{variance_components.csv},
and a figure into \verb{output_dir/<model_def$name>/}.
}
