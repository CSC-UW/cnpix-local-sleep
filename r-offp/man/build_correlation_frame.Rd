% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_analysis.R
\name{build_correlation_frame}
\alias{build_correlation_frame}
\title{Build the per-(subject, probe, structure) correlation frame}
\usage{
build_correlation_frame(
  d,
  metric,
  predictor_condition = PREDICTOR_CONDITION,
  rebound_post = REBOUND_POST,
  rebound_baseline = REBOUND_BASELINE,
  response_metric = metric,
  response_data = NULL
)
}
\arguments{
\item{d}{Data frame from \code{\link[=load_nod_rebound_correlation_data]{load_nod_rebound_correlation_data()}}; supplies \code{x}.}

\item{metric}{Predictor (x) metric column name (e.g. \code{"total_area_norm"},
\code{"rate"}).}

\item{predictor_condition}{Predictor (x) condition (default
\link{PREDICTOR_CONDITION}).}

\item{rebound_post, rebound_baseline}{The two conditions whose difference is the
response \code{y} (defaults \link{REBOUND_POST}, \link{REBOUND_BASELINE}).}

\item{response_metric}{Response (y) metric column name (default \code{metric}).}

\item{response_data}{Optional frame supplying \code{response_metric} at the rebound
conditions. \code{NULL} (default) means take the response from \code{d} too.}
}
\value{
Data frame with columns \code{subject} (factor), \code{probe}, \code{structure}
(factor), \code{x}, \code{y}.
}
\description{
Reshapes the long correlation input into one row per
\verb{(subject, probe, structure)} with \code{x} (the predictor metric in the NOD
window) and \code{y} (a metric's \code{NREM.Rebound}, i.e.
\code{response_metric@REBOUND_POST - response_metric@REBOUND_BASELINE}). Restricts
to \code{clade == "Cx"} (when present) and drops combos missing any of the required
conditions. Mirrors f25a's \code{get_correlation_data} plus the rebound difference.
}
\details{
By default \code{response_metric == metric} and \code{response_data == NULL}, so \code{x} and
\code{y} are the \emph{same} metric taken from the same frame \code{d} (the original
self-rebound case). To correlate an OFF predictor with the \code{NREM.Rebound} of a
\emph{different} quantity (e.g. \code{mean_zlog_delta} cortical delta power), pass a
distinct \code{response_metric} and a \code{response_data} frame carrying that column at
the rebound conditions (the band-power condition means from
\code{\link[=load_bandpower_condition_means]{load_bandpower_condition_means()}}); it is joined to \code{x} on
\verb{(subject, probe, structure)}.
}
