% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locality_analysis.R
\name{build_locality_simple_effect_contrasts}
\alias{build_locality_simple_effect_contrasts}
\title{Build per-condition simple-effect contrasts for overlap_status}
\usage{
build_locality_simple_effect_contrasts(conditions, full_model)
}
\arguments{
\item{conditions}{Condition (or state) levels in the set; the first is the
factor reference level.}

\item{full_model}{The fitted interaction model.}
}
\value{
A numeric contrast matrix (one row per condition), or \code{NULL}.
}
\description{
For each condition in the set, builds the linear combination of fixed effects
equal to the simple effect of \code{overlap_status} \emph{within that condition}
(\code{Overlapping - Local | condition}). This is the pooled-model analogue of the
per-condition \link{run_locality_main_effect_analysis} fits: instead of fitting one
model per condition, the simple effects are read off the single
\code{condition * overlap_status} interaction model as \code{multcomp} contrasts, so
they share one pooled residual/RE variance and a single family-wise
multiplicity adjustment. With \code{Local} as the overlap_status reference and
treatment contrasts, the simple effect in the reference condition is
\code{coef(overlap_statusOverlapping)}, and in any other condition C it is
\verb{coef(overlap_statusOverlapping) + coef(condition<C>:overlap_statusOverlapping)}.
}
