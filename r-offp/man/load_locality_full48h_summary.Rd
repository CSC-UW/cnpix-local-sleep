% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locality_data_loading.R
\name{load_locality_full48h_summary}
\alias{load_locality_full48h_summary}
\title{Load the whole-recording NREM/Wake Local-vs-Overlapping summary (request 3)}
\usage{
load_locality_full48h_summary(off_type = "llas")
}
\arguments{
\item{off_type}{Only \code{"llas"} is exported.}
}
\value{
Data frame with the whole-recording by-state, by-overlap_status metrics.
}
\description{
Reads \verb{summarized_locality_full48h_<off_type>_offs.parquet}: one row per
(subject, probe, structure, state, overlap_status) with \verb{state in \{NREM, Wake\}}
and the same per-group medians, computed on the whole-recording (not
condition-subset) OFFs. The \code{condition} column equals \code{state} so the runner's
\verb{conditions=} filter works unchanged.
}
