% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_loading.R
\name{join_adjusted_edge_statistics}
\alias{join_adjusted_edge_statistics}
\title{Join size-adjusted onset/offset MAD columns, when they have been exported}
\usage{
join_adjusted_edge_statistics(d, off_type)
}
\arguments{
\item{d}{Summary data frame from \link{load_offs_summary}.}

\item{off_type}{One of \link{OFF_TYPES}.}
}
\value{
\code{d}, with \verb{adj_*} columns appended where available.
}
\description{
Onset/offset MAD depend on how many channels an OFF period spans for reasons
that are mechanical rather than neural (the detector's spatial structuring
element forces \code{MAD == 0} below a 120 um span, and the estimator keeps
climbing with span above it), so a condition effect on the raw statistic
partly reflects a condition effect on OFF \emph{size}. cnpix_local_sleep's
\verb{off-analysis export-adjusted-edge-statistics} re-estimates each cell mean by
marginal standardization (the g-formula) and writes
\verb{summarized_full48h_<off_type>_edge_adjusted.parquet}.
}
\details{
This helper left-joins the \verb{adj_*} columns onto the summary frame when that
file is present, and is a no-op when it is not, so nothing breaks if the
companion export has not been run.
}
\keyword{internal}
