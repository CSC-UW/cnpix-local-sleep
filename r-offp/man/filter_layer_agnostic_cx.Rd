% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_loading.R
\name{filter_layer_agnostic_cx}
\alias{filter_layer_agnostic_cx}
\title{Restrict a summary frame to cortical, layer-agnostic, spatial OFFs}
\usage{
filter_layer_agnostic_cx(d)
}
\arguments{
\item{d}{Summary data frame, e.g. from \code{\link[=load_offs_summary]{load_offs_summary()}}.}
}
\value{
\code{d}, restricted to the cortical layer-agnostic spatial rows.
}
\description{
The analysis frame is always the cortical (\code{clade == "Cx"}), layer-agnostic,
spatially-detected subset. \code{layer} and \code{detection_mode} are WNE \emph{path}
components rather than summary columns: the cnpix_local_sleep summarizer groups by
\code{subject}/\code{probe}/\code{structure}/\code{condition} only, so current exports omit both
columns and every row is \code{None}/\code{spatial} by construction. Older exports still
carry them, so each filter applies only when its column is present.
}
\details{
Single point of truth for that restriction, shared by
\code{\link[=run_cx_homeostasis_analysis]{run_cx_homeostasis_analysis()}} and the test suite.
}
\keyword{internal}
