% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{fit_models}
\alias{fit_models}
\title{Fit full and null models for given fixed and random effect terms}
\usage{
fit_models(d, response_var, fe_terms, re_terms, weights = NULL)
}
\arguments{
\item{d}{Data frame containing the response variable and all
columns referenced in \code{fe_terms} and \code{re_terms}.}

\item{response_var}{Response variable name (string).}

\item{fe_terms}{Length-1 character vector specifying the fixed
effect term (e.g., \code{"condition"}).}

\item{re_terms}{Character vector of random effect terms (e.g.,
\code{c("(1 | subject)", "(1 | structure)")}).}

\item{weights}{Optional numeric vector of prior weights for
weighted estimation. Passed directly to \code{\link[lme4:lmer]{lme4::lmer()}}. If
\code{NULL} (default), unweighted estimation is used.}
}
\value{
Named list with elements \code{full} and \code{null}, each a fitted
\code{lmerMod} object.
}
\description{
Fits a full model (with the specified fixed effect) and a null model
(intercept-only fixed effects) using the provided random effects
structure. Both models are fit with ML estimation (\code{REML = FALSE})
to enable likelihood ratio testing.
}
