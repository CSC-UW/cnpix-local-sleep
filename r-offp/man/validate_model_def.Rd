% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{validate_model_def}
\alias{validate_model_def}
\title{Validate a model definition list}
\usage{
validate_model_def(model_def)
}
\arguments{
\item{model_def}{A list with elements \code{name}, \code{fe_terms},
\code{re_terms}, \code{weighted}, and optionally \code{transform}.}
}
\value{
\code{model_def} invisibly (called for side effects).
}
\description{
Checks that a model definition list contains the required fields
with correct types. Stops with an informative error if validation
fails.
}
\details{
A model definition must have:
\itemize{
\item \code{name}: a single string identifying the model (used for output
directory naming)
\item \code{fe_terms}: a length-1 character vector of fixed effect terms.
Only a single fixed effect term is supported because the null
model is derived by removing all \code{fe_terms}; with multiple FE
terms, the LRT would test all of them simultaneously.
\item \code{re_terms}: a character vector of random effect terms (e.g.,
\code{c("(1 | subject)", "(1 | structure)")})
\item \code{weighted}: a logical flag for inverse-variance weighting
\item \code{transform} (optional): name of a response transform applied to the
response column before weighting/fitting; one of
\code{names(RESPONSE_TRANSFORMS)}. Defaults to \code{"identity"} when absent.
}
}
