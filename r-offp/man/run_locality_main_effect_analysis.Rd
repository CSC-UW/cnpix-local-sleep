% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locality_analysis.R
\name{run_locality_main_effect_analysis}
\alias{run_locality_main_effect_analysis}
\title{Run a locality Local-vs-Overlapping analysis for one response variable}
\usage{
run_locality_main_effect_analysis(
  response_var,
  output_dir,
  model_def,
  off_type = "llas",
  analysis_kind = NULL,
  conditions = NULL,
  locality_posthocs = c("Overlapping - Local = 0"),
  data = NULL,
  save_ggplot_rds = TRUE
)
}
\arguments{
\item{response_var}{Response variable column name.}

\item{output_dir}{Base directory; a \verb{<model_def$name>/} subdir is created.}

\item{model_def}{Model definition (\code{name}, \code{fe_terms = "overlap_status"},
\code{re_terms}, \code{weighted}, optional \code{transform}).}

\item{off_type}{One of \link{OFF_TYPES} (provenance label; default \code{"llas"}).}

\item{analysis_kind}{Provenance label recorded in the \code{condition_set} slot
(e.g. \code{"cond-Early.REC.NREM"}, \code{"NREM"}, \code{"Wake"}).}

\item{conditions}{Character vector of condition/state levels to include.}

\item{locality_posthocs}{Contrast strings for the Local-vs-Overlapping posthoc.}

\item{data}{Pre-loaded + prepared locality data frame.}

\item{save_ggplot_rds}{If \code{TRUE}, also save ggplot objects as \code{.rds}.}
}
\value{
Invisibly, the full result list (also saved as \code{results.rds}).
}
\description{
Mirrors \code{\link[=run_cx_homeostasis_analysis]{run_cx_homeostasis_analysis()}} but tests \code{overlap_status} (Local vs
Overlapping) as the fixed effect. Use \code{conditions} to select the comparison
window: a single statistical condition (request 2) or a single whole-recording
state, \code{"NREM"} or \code{"Wake"} (request 3). When more than one condition is pooled,
supply a model whose \code{re_terms} include a \code{(1 | subject:structure:condition)}
cell so that \code{overlap_status} is a pure within-cell (paired) contrast.
}
