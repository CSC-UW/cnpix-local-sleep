% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_tests.R
\name{test_main_effect}
\alias{test_main_effect}
\title{Test for main effect of condition}
\usage{
test_main_effect(dat, models, contrast_matrix = NULL, weights = NULL)
}
\arguments{
\item{dat}{Data frame.}

\item{models}{Named list with elements \code{full} and \code{null}.}

\item{contrast_matrix}{Optional contrast matrix for post-hoc tests (a
\code{multcomp::mcp} object).}

\item{weights}{Optional numeric vector of prior weights, passed through
to \code{\link[=subtract_ranef_get_fsquared]{subtract_ranef_get_fsquared()}}.}
}
\value{
List with main effect test results: \code{anova}, \code{pval}, and
optionally \code{effect_size} and \code{posthoc}.
}
\description{
Compares the full model (with \code{condition} as a fixed effect) to the null
model (intercept-only fixed effects) via likelihood ratio test. If the
main effect is significant and a contrast matrix is provided, performs
post-hoc tests via \code{\link[multcomp:glht]{multcomp::glht()}}.
}
