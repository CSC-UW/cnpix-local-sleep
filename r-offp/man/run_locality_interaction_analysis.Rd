% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locality_analysis.R
\name{run_locality_interaction_analysis}
\alias{run_locality_interaction_analysis}
\title{Run a locality condition x overlap_status interaction analysis}
\usage{
run_locality_interaction_analysis(
  response_var,
  output_dir,
  model_def,
  off_type = "llas",
  condition_set = NULL,
  conditions = NULL,
  posthocs = NULL,
  data = NULL,
  save_ggplot_rds = TRUE
)
}
\arguments{
\item{response_var}{Response variable column name.}

\item{output_dir}{Base directory; a \verb{<model_def$name>/} subdir is created.}

\item{model_def}{Model definition (\code{name}, \code{re_terms}, \code{weighted}, optional
\code{transform}; \code{fe_terms} is descriptive only here).}

\item{off_type}{One of \link{OFF_TYPES}.}

\item{condition_set}{Name of the condition/state set (e.g. \code{"nrem"}, \code{"state"}).}

\item{conditions}{Condition/state levels in the set.}

\item{posthocs}{Condition (or state) contrast strings for the DiD.}

\item{data}{Pre-loaded + prepared locality data frame.}

\item{save_ggplot_rds}{If \code{TRUE}, also save ggplot objects as \code{.rds}.}
}
\value{
Invisibly, the full result list (also saved as \code{results.rds}).
}
\description{
Fits the interaction model, LR-tests the interaction, computes the omnibus
Cohen's f^2, and computes a difference-in-differences contrast for each
condition posthoc. It additionally reads off, from the SAME pooled model, the
per-condition simple effects of \code{overlap_status} (the pooled-variance
alternative to the separate per-condition \link{run_locality_main_effect_analysis}
fits) and the marginal main effect of \code{overlap_status} (averaged across
conditions), so the two approaches can be compared head-to-head.
}
