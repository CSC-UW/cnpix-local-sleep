% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{run_cx_homeostasis_analysis}
\alias{run_cx_homeostasis_analysis}
\title{Run a cx_homeostasis analysis for one response variable}
\usage{
run_cx_homeostasis_analysis(
  response_var,
  output_dir,
  model_def,
  off_type = "llas",
  condition_set = NULL,
  conditions = NULL,
  posthocs = NULL,
  data = NULL,
  save_ggplot_rds = TRUE
)
}
\arguments{
\item{response_var}{Column name of the response variable (string).}

\item{output_dir}{Base directory for results. A subdirectory named
after the model definition's \code{name} is created within it.}

\item{model_def}{Model definition list with elements \code{name},
\code{fe_terms}, \code{re_terms}, and \code{weighted}. See
\code{\link[=validate_model_def]{validate_model_def()}} for details.}

\item{off_type}{Type of OFFs to analyze: one of \link{OFF_TYPES}.}

\item{condition_set}{Optional name of the condition set this analysis
belongs to (e.g. \code{"six"}, \code{"nrem"}, \code{"wake"}). Recorded in the result /
JSON / text outputs for provenance and consumed by the plot/summary
tooling; does not itself change which rows are fit (use \code{conditions} /
\code{posthocs} for that).}

\item{conditions}{Character vector of conditions to include, or \code{NULL}
for all conditions present in the data.}

\item{posthocs}{Character vector of contrast strings for post-hoc tests,
e.g. \code{c("Early.REC.NREM - Early.BSL.NREM = 0", ...)}. If \code{NULL}, no
post-hoc tests are performed.}

\item{data}{Optional pre-loaded data frame. If \code{NULL}, calls
\code{\link[=load_offs_summary]{load_offs_summary()}} and applies the standard Cx/None/spatial filter.}

\item{save_ggplot_rds}{If \code{TRUE}, save ggplot objects as \code{.rds} alongside
PNGs for programmatic inspection.}
}
\value{
Invisibly, the full result list (also saved as \code{results.rds}).
}
\description{
Executes the full pipeline: load -> filter -> fit -> test -> plot -> save.
Results are saved to \verb{<output_dir>/<model_def$name>/} as RDS, JSON, PNG,
and text files.
}
