% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_analysis.R
\name{load_nod_rebound_correlation_data}
\alias{load_nod_rebound_correlation_data}
\title{Load the NOD-rebound correlation input data}
\usage{
load_nod_rebound_correlation_data(off_type = "llas")
}
\arguments{
\item{off_type}{One of \link{OFF_TYPES}.}
}
\value{
Data frame with the summarized metrics for the correlation conditions.
}
\description{
Reads \verb{nod_rebound_correlation_<off_type>_offs.parquet} from the package
\code{extdata}. Produced by cnpix_local_sleep's \verb{off-analysis export-nod-rebound-correlation},
it carries the same summarized schema as \code{\link[=load_offs_summary]{load_offs_summary()}} but for the
three correlation conditions (\code{NOD}, \code{Early.REC.NREM}, \code{Early.REC.NREM.Match}),
one row per \verb{(subject, probe, structure, condition)}. A dedicated loader
rather than an option on \code{\link[=load_offs_summary]{load_offs_summary()}}, so the pipeline stays fully
separable.
}
